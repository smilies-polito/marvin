# GDB command file to execute a program N times and set a breakpoint in a random place

# Set the number of times to execute the program
set $n = 10

# Loop through the number of times to execute the program
while ($n > 0)
  # Set a breakpoint in a random place
  python3 import random; break_line = random.choice(gdb.execute("info line").splitlines());gdb.execute("break %s" % break_line.split(':')[0])
  #break 158
  # Start the program
  run

  # Decrease the number of times to execute the program
  set $n = $n - 1

  # Continue execution until the breakpoint is hit
  continue
end
